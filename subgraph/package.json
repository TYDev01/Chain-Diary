{
  "name": "chain-diary-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Chain Diary",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:local": "graph deploy --node http://localhost:8020/ chain-diary-subgraph",
    "deploy:studio": "graph deploy --studio chain-diary-subgraph",
    "create:local": "graph create --node http://localhost:8020/ chain-diary-subgraph",
    "remove:local": "graph remove --node http://localhost:8020/ chain-diary-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
